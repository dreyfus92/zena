{
  "name": "@zena-lang/zena-run",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "wireit",
    "test": "wireit",
    "zena-run": "wireit"
  },
  "wireit": {
    "zena-run": {
      "command": "../../target/release/zena-run",
      "dependencies": [
        "build"
      ]
    },
    "build": {
      "command": "cargo build --release -p zena-run",
      "#files": "The crate depends on ../zena-runtime, so its sources are inputs too. The workspace manifest and lockfile at the repo root pin every dependency version.",
      "files": [
        "Cargo.toml",
        "src/**/*.rs",
        "../zena-runtime/Cargo.toml",
        "../zena-runtime/src/**/*.rs",
        "../../Cargo.toml",
        "../../Cargo.lock"
      ],
      "#output": "The binary lands in the workspace target dir (../../target/release/zena-run); cargo tracks its own staleness, so wireit declares no output and only skips the script when no input changed.",
      "output": [],
      "dependencies": []
    },
    "test": {
      "command": "cargo test --release -p zena-run",
      "#command": "--release reuses the dependency artifacts the build step produced instead of compiling wasmtime a second time in the dev profile.",
      "files": [
        "Cargo.toml",
        "src/**/*.rs",
        "tests/**/*.rs",
        "../zena-runtime/Cargo.toml",
        "../zena-runtime/src/**/*.rs",
        "../../Cargo.toml",
        "../../Cargo.lock"
      ],
      "output": [],
      "dependencies": [
        "build"
      ]
    }
  }
}
