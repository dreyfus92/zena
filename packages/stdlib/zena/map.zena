// TRANSITIONAL. This module has no manifest entry and nothing imports it.
// It exists only so the checked-in bootstrap can build the compiler: the
// prelude baked into that bootstrap still names 'zena:map', so it loads
// this file unconditionally, and a missing file is a hard read error
// rather than an unresolved import. See "The Bootstrap" in AGENTS.md.
//
// Delete this file after the next reseed. Nothing else needs to change;
// the manifests and the prelude already name 'zena:collections'.

// The unordered map, from the collections library.

export { Hashable } from 'zena:core';

export { Map, MapEntry } from './collections/map.zena';
export { HashMap } from './collections/hash-map.zena';
