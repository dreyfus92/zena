# Cases from urltestdata.json that zena:url does not pass yet.
# Each line is JSON [input, base] — the key the generator matches on, so the
# list survives upstream reordering. Listed cases are emitted as testSkip and
# stay visible in the test output; burning this list down is the conformance
# metric for each phase.
#
# ALL entries below need IDNA / UTS 46 host processing (DESIGN.md phase 6).
# There are no other known failures.

# non-ASCII in host and userinfo (needs ToASCII/punycode)
# zero-width code points removed by UTS 46 mapping
# U+3002 ideographic full stop maps to '.' under UTS 46
# fullwidth letters map to ASCII under UTS 46
# non-ASCII domain needs punycode encoding
# sharp-s needs UTS 46 non-transitional mapping
# fullwidth digits map to ASCII, then parse as IPv4
# percent-decoded host is non-ASCII (needs ToASCII)
# percent-decoded host is non-ASCII (needs ToASCII)
# soft hyphen U+00AD is ignored by UTS 46 mapping
# percent-decoded soft hyphen is ignored by UTS 46 mapping
# percent-decoded soft hyphen is ignored by UTS 46 mapping

# Setter cases, keyed ["setter", property, href, new_value]. All three need
# IDNA / UTS 46 host processing, same as the parser cases above.

# ---------------------------------------------------------------------------
# toascii.json, keyed ["toascii", input]. Every entry is a UTS 46 rule that
# idna.zena does not implement yet; nothing here is silently wrong, each is a
# host we accept that a fully conformant implementation would reject.
# ---------------------------------------------------------------------------
# NFC normalization (UTS 46 step 2) is not implemented.
["toascii","≠"]
["toascii","≮"]
["toascii","≯"]
["toascii","=­̸"]
["toascii","<­̸"]
["toascii",">­̸"]
["toascii","looḱout.net"]
["toascii","ড়.com"]
["toascii","foò.bar.com"]

# CheckBidi is not implemented.
["toascii","يa"]
["toascii","look־out.net"]

# CheckJoiners is not implemented.
["toascii","‍.example"]

# we do not apply the UTS 46 validity criteria to a decoded ACE label; see idna.zena.
["toascii","xn--a.ß"]
["toascii","xn--ls8h="]
["toascii","xn--0.com"]

# ---------------------------------------------------------------------------
# urlpatterntestdata.json, keyed ["urlpattern", <the case's pattern array>].
#
# Only cases that are EMITTED and fail belong here. The pattern forms the Zena
# API cannot express at all — a pattern given as a string, a lone surrogate, an
# init object passed alongside a base URL — are never emitted, and are reported
# as counts with their reasons at the end of a generation run instead. See
# README.md.
# ---------------------------------------------------------------------------

# A group name may be any \p{ID_Start} followed by \p{ID_Continue}, and this
# parser accepts ASCII identifiers only: zena:regex has no property escapes,
# and the tables to decide it properly are the size of the IDNA one. Note that
# "is it a letter" would not be enough either — `é` and `㐀` are letters, but
# `℘` qualifies only through Other_ID_Start.
["urlpattern",[{"protocol":":café"}]]
["urlpattern",[{"username":":café"}]]
["urlpattern",[{"password":":café"}]]
["urlpattern",[{"hostname":":café"}]]
["urlpattern",[{"pathname":"/:café"}]]
["urlpattern",[{"search":":café"}]]
["urlpattern",[{"hash":":café"}]]
["urlpattern",[{"protocol":":℘"}]]
["urlpattern",[{"username":":℘"}]]
["urlpattern",[{"password":":℘"}]]
["urlpattern",[{"hostname":":℘"}]]
["urlpattern",[{"pathname":"/:℘"}]]
["urlpattern",[{"search":":℘"}]]
["urlpattern",[{"hash":":℘"}]]
["urlpattern",[{"protocol":":㐀"}]]
["urlpattern",[{"username":":㐀"}]]
["urlpattern",[{"password":":㐀"}]]
["urlpattern",[{"hostname":":㐀"}]]
["urlpattern",[{"pathname":"/:㐀"}]]
["urlpattern",[{"search":":㐀"}]]
["urlpattern",[{"hash":":㐀"}]]

# zena:regex reads `\m` as a literal `m`; a JavaScript RegExp with the `u`
# flag rejects it. The constructor compiles the protocol pattern only, so
# nothing here even reaches the regex engine yet — this case will keep failing
# until exec() compiles every component and the engine rejects the source.
["urlpattern",[{"pathname":"/(\\m)"}]]

