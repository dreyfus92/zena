// @mode: check

class Point {
  x: i32;
  y: i32;

  new(this.x, this.y);

  new origin() : x = 0, y = 0;

  new fromX(x: i32) : x = x, y = 0;
}

let p1 = new Point(1, 2);
let p2 = new Point.origin();
let p3 = new Point.fromX(5);

