// @mode: check
import {Error} from 'zena:core';
// Basic try/catch is valid; body and handler are both checked.
let fn = () => {};

let test = () => {
  try {
    fn();
    throw new Error('oops');
  } catch (e) {
    // swallow
  };
};
