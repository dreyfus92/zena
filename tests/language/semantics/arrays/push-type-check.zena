// @mode: check
// Pushing an element of the wrong type into Array<i32> is rejected.
import {GrowableArray} from 'zena:core';
let arr = new GrowableArray<i32>();
arr.push("hello"); // @error: Type mismatch
