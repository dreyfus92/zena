// @mode: check
// An empty array literal infers its element type from the contextual type annotation.
// Array literals are typed as FixedArray<T>, so we use that here.
import {FixedArray} from 'zena:core';
let arr: FixedArray<i32> = [];
