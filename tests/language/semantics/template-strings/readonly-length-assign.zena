// @mode: check
// TemplateStringsArray.length should reject assignment (getter-only)
import {TemplateStringsArray} from 'zena:core';

let tag = (strings: TemplateStringsArray, values: FixedArray<i32>): i32 => {
  strings.length = 5; // @error: Cannot assign
  return 0;
};
