// @mode: check
// for-in loop directly on an Iterator<T>
import { HashMap } from 'zena:collections';
import {Box} from 'zena:core';
let map = new HashMap<String, Box<i32>>();
for (let key in map.keys()) {
  let s: String = key;
}
