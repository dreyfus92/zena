// @mode: check
import { Box } from 'zena:core';

class Point {
  x: i32;
  new(this.x);
}

let a: anyref = new Point(1);
let b: anyref = 'hello';
let c: anyref = null;
let d: anyref = new Box<i32>(7);
